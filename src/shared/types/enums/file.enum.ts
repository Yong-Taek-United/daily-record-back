export enum UserFileType {
  PROFILE = 'PROFILE',
  FEED = 'FEED',
}

export enum FileStorageType {
  DISK = 'DISK',
  S3 = 'S3',
}

export enum MimeType {
  JPEG = 'image/jpeg',
  PNG = 'image/png',
  ICO = 'image/ico',
  WEBP = 'image/webp',
  GIF = 'image/gif',
  BMP = 'image/bmp',
}

export enum IconType {
  TASK = 'TASK',
  MEDAL = 'MEDAL',
}
