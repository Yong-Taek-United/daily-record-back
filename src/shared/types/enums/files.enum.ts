export enum UserFileType {
  PROFILE = 'PROFILE',
  FEED = 'FEED',
}

export enum FileStorageType {
  DISK = 'DISK',
  S3 = 'S3',
}

export enum MimeType {
  JPEG = 'image/jpeg',
  PNG = 'image/png',
  ICO = 'image/ico',
}

export enum IconType {
  TASK = 'TASK',
  MEDAL = 'MEDAL',
}
