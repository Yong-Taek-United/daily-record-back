export enum UserFileType {
  PROFILE = 'PROFILE',
  FEED = 'FEED',
}

export enum MimeType {
  JPEG = 'image/jpeg',
  PNG = 'image/png',
  ICO = 'image/ico',
}

export enum IconType {
  TASK = 'TASK',
  MEDAL = 'MEDAL',
}
